export const Banner = () => {
  return (
    <section className="flex justify-center bg-foreground p-2">
      <span className="text-center text-sm font-light text-muted/70">
        This is a reference implementation of the Grids data which is still in
        ALPHA - Data is focused on Solana projects - API users can pick and show
        data relevant to their users.
      </span>
    </section>
  );
};
